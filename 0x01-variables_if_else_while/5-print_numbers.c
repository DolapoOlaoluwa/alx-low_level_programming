#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 always success
 */

int main(void)
{
	int digit = 0;

	for (; digit < 10; digit++)
	{
		printf("%d", digit);
	}
	printf("\n");
	return (0);
}
